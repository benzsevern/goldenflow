# GoldenFlow Examples

| Script | Description |
|--------|-------------|
| `transform_basic.py` | Zero-config transform -- auto-detect and fix quality issues |
| `config_based.py` | Transform with a YAML config for explicit control |
| `domain_pack.py` | Healthcare domain transforms for clinical data |

## Prerequisites

```bash
pip install goldenflow
```
