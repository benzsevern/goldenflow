{
  "private": true,
  "description": "Not a real npm workspace — each package installs independently to avoid Windows symlink issues.",
  "scripts": {
    "install:js": "npm --prefix packages/goldenflow-js install",
    "build:js": "npm --prefix packages/goldenflow-js run build",
    "test:js": "npm --prefix packages/goldenflow-js run test",
    "typecheck:js": "npm --prefix packages/goldenflow-js run typecheck"
  }
}
